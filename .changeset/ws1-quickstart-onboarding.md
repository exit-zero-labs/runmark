---
"@exit-zero-labs/runmark": minor
---

Add `runmark quickstart` for one-command onboarding (scaffold + bundled demo + smoke run + teardown), let `runmark run` and `runmark describe` auto-pick the sole run when no target is specified, and emit one-line human hints on stderr after `init`, `run`, `resume`, and `quickstart` so interactive users know the next command without losing JSON stdout for automation.
