# yaml-language-server: $schema=https://raw.githubusercontent.com/exit-zero-labs/runmark/main/packages/contracts/schemas/request.schema.json
kind: request
title: Create cart
method: POST
url: "{{baseUrl}}/commerce/carts"
auth:
  scheme: header
  header: x-api-key
  value: $ENV:COMMERCE_API_TOKEN
body:
  file: carts/create-cart.json
  contentType: application/json
expect:
  status: 201
extract:
  cartId:
    from: $.id
    required: true
